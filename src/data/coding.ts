import type { CodingQuestion } from '@/types';

export const codingQuestions: CodingQuestion[] = [
  {
    id: 'code-1', title: 'Prime Number', difficulty: 'easy',
    statement: 'Check whether a given number is prime. A prime number is greater than 1 and divisible only by 1 and itself.',
    approach: 'Check divisibility from 2 up to the square root of the number.',
    logic: 'If n < 2, not prime. Otherwise loop i from 2 to sqrt(n); if n % i == 0, not prime.',
    dryRun: 'n = 7: check 2 (7%2!=0), check 3 (7%3!=0). No divisor up to sqrt(7)≈2.6, so prime.',
    java: 'public static boolean isPrime(int n) {\n  if (n < 2) return false;\n  for (int i = 2; i * i <= n; i++) {\n    if (n % i == 0) return false;\n  }\n  return true;\n}',
    python: 'def is_prime(n):\n    if n < 2:\n        return False\n    for i in range(2, int(n**0.5) + 1):\n        if n % i == 0:\n            return False\n    return True',
    cpp: 'bool isPrime(int n) {\n  if (n < 2) return false;\n  for (int i = 2; i * i <= n; i++) {\n    if (n % i == 0) return false;\n  }\n  return true;\n}',
    timeComplexity: 'O(sqrt(n))',
    bestCase: 'O(1)',
    avgCase: 'O(sqrt(n))',
    worstCase: 'O(sqrt(n))',
    spaceComplexity: 'O(1)',
    explanation: 'We only check up to sqrt(n) because if n has a factor, one must be <= sqrt(n).',
  },
  {
    id: 'code-2', title: 'Palindrome Number', difficulty: 'easy',
    statement: 'Check if a number reads the same forwards and backwards (e.g., 121).',
    approach: 'Reverse the number and compare with the original.',
    logic: 'Build the reversed number by extracting digits from the end and accumulating.',
    dryRun: 'n = 121: reversed = 0 -> 1 -> 12 -> 121. reversed == original, so palindrome.',
    java: 'public static boolean isPalindrome(int n) {\n  int original = n, reversed = 0;\n  while (n > 0) {\n    reversed = reversed * 10 + n % 10;\n    n /= 10;\n  }\n  return original == reversed;\n}',
    python: 'def is_palindrome(n):\n    return str(n) == str(n)[::-1]',
    cpp: 'bool isPalindrome(int n) {\n  int original = n, reversed = 0;\n  while (n > 0) {\n    reversed = reversed * 10 + n % 10;\n    n /= 10;\n  }\n  return original == reversed;\n}',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Reversing extracts each digit once; number of digits is log10(n).',
  },
  {
    id: 'code-3', title: 'Armstrong Number', difficulty: 'easy',
    statement: 'Check if a number equals the sum of its own digits each raised to the power of the number of digits (e.g., 153 = 1^3 + 5^3 + 3^3).',
    approach: 'Count digits, then sum each digit raised to that count.',
    logic: 'Loop through digits, raise each to the digit count, sum, and compare.',
    dryRun: 'n = 153: 1^3 + 5^3 + 3^3 = 1 + 125 + 27 = 153. Equal, so Armstrong.',
    java: 'public static boolean isArmstrong(int n) {\n  int sum = 0, temp = n, digits = String.valueOf(n).length();\n  while (temp > 0) {\n    int d = temp % 10;\n    sum += Math.pow(d, digits);\n    temp /= 10;\n  }\n  return sum == n;\n}',
    python: 'def is_armstrong(n):\n    digits = len(str(n))\n    return n == sum(int(d) ** digits for d in str(n))',
    cpp: 'bool isArmstrong(int n) {\n  int sum = 0, temp = n;\n  int digits = std::to_string(n).length();\n  while (temp > 0) {\n    int d = temp % 10;\n    sum += pow(d, digits);\n    temp /= 10;\n  }\n  return sum == n;\n}',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'We process each digit once; the count of digits is log10(n).',
  },
  {
    id: 'code-4', title: 'Fibonacci Series', difficulty: 'easy',
    statement: 'Print the first n Fibonacci numbers, where each is the sum of the previous two.',
    approach: 'Iteratively keep the last two values and build the next.',
    logic: 'Start with 0 and 1; each next term is the sum of the previous two.',
    dryRun: 'n = 5: 0, 1, 1, 2, 3.',
    java: 'public static void fibonacci(int n) {\n  int a = 0, b = 1;\n  for (int i = 0; i < n; i++) {\n    System.out.print(a + " ");\n    int next = a + b;\n    a = b;\n    b = next;\n  }\n}',
    python: 'def fibonacci(n):\n    a, b = 0, 1\n    for _ in range(n):\n        print(a, end=" ")\n        a, b = b, a + b',
    cpp: 'void fibonacci(int n) {\n  int a = 0, b = 1;\n  for (int i = 0; i < n; i++) {\n    std::cout << a << " ";\n    int next = a + b;\n    a = b;\n    b = next;\n  }\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'Each of the n terms is computed in constant time.',
  },
  {
    id: 'code-5', title: 'Factorial', difficulty: 'easy',
    statement: 'Compute n! = n * (n-1) * ... * 1.',
    approach: 'Multiply from 1 to n iteratively (or recursively).',
    logic: 'Accumulate the product from 1 up to n.',
    dryRun: 'n = 5: 1*2*3*4*5 = 120.',
    java: 'public static long factorial(int n) {\n  long result = 1;\n  for (int i = 2; i <= n; i++) result *= i;\n  return result;\n}',
    python: 'def factorial(n):\n    result = 1\n    for i in range(2, n + 1):\n        result *= i\n    return result',
    cpp: 'long long factorial(int n) {\n  long long result = 1;\n  for (int i = 2; i <= n; i++) result *= i;\n  return result;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'We perform n-1 multiplications.',
  },
  {
    id: 'code-6', title: 'Largest of Three Numbers', difficulty: 'easy',
    statement: 'Find the largest of three given numbers.',
    approach: 'Compare pairs using if-else or the ternary operator.',
    logic: 'Compare a with b and c, then b with c if needed.',
    dryRun: 'a=3, b=7, c=5: 7 > 3 and 7 > 5, so largest is 7.',
    java: 'public static int largest(int a, int b, int c) {\n  return Math.max(a, Math.max(b, c));\n}',
    python: 'def largest(a, b, c):\n    return max(a, b, c)',
    cpp: 'int largest(int a, int b, int c) {\n  return std::max(a, std::max(b, c));\n}',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'A fixed number of comparisons regardless of input.',
  },
  {
    id: 'code-7', title: 'Reverse Number', difficulty: 'easy',
    statement: 'Reverse the digits of a number (e.g., 123 -> 321).',
    approach: 'Extract digits from the end and build a reversed number.',
    logic: 'Repeatedly take n % 10 and append to the reversed result.',
    dryRun: 'n = 123: 0*10+3=3, 3*10+2=32, 32*10+1=321.',
    java: 'public static int reverse(int n) {\n  int reversed = 0;\n  while (n != 0) {\n    reversed = reversed * 10 + n % 10;\n    n /= 10;\n  }\n  return reversed;\n}',
    python: 'def reverse(n):\n    return int(str(n)[::-1])',
    cpp: 'int reverse(int n) {\n  int reversed = 0;\n  while (n != 0) {\n    reversed = reversed * 10 + n % 10;\n    n /= 10;\n  }\n  return reversed;\n}',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Number of digits is log10(n).',
  },
  {
    id: 'code-8', title: 'Sum of Digits', difficulty: 'easy',
    statement: 'Compute the sum of the digits of a number (e.g., 123 -> 6).',
    approach: 'Extract each digit with modulo and add to a sum.',
    logic: 'While n > 0, add n % 10 to the sum and divide n by 10.',
    dryRun: 'n = 123: 3 + 2 + 1 = 6.',
    java: 'public static int sumDigits(int n) {\n  int sum = 0;\n  while (n != 0) {\n    sum += n % 10;\n    n /= 10;\n  }\n  return sum;\n}',
    python: 'def sum_digits(n):\n    return sum(int(d) for d in str(n))',
    cpp: 'int sumDigits(int n) {\n  int sum = 0;\n  while (n != 0) {\n    sum += n % 10;\n    n /= 10;\n  }\n  return sum;\n}',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'We process each digit once.',
  },
  {
    id: 'code-9', title: 'Count Digits', difficulty: 'easy',
    statement: 'Count the number of digits in a number (e.g., 12345 -> 5).',
    approach: 'Divide by 10 repeatedly until the number is 0.',
    logic: 'Increment a counter each time you divide by 10.',
    dryRun: 'n = 12345: 1234, 123, 12, 1, 0 -> 5 divisions.',
    java: 'public static int countDigits(int n) {\n  if (n == 0) return 1;\n  int count = 0;\n  while (n != 0) {\n    count++;\n    n /= 10;\n  }\n  return count;\n}',
    python: 'def count_digits(n):\n    return len(str(abs(n)))',
    cpp: 'int countDigits(int n) {\n  if (n == 0) return 1;\n  int count = 0;\n  while (n != 0) {\n    count++;\n    n /= 10;\n  }\n  return count;\n}',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Each division by 10 removes one digit.',
  },
  {
    id: 'code-10', title: 'Even or Odd', difficulty: 'easy',
    statement: 'Check whether a number is even or odd.',
    approach: 'Use the modulo operator with 2.',
    logic: 'If n % 2 == 0, even; otherwise odd.',
    dryRun: 'n = 4: 4 % 2 == 0, so even. n = 7: 7 % 2 == 1, so odd.',
    java: 'public static String evenOdd(int n) {\n  return (n % 2 == 0) ? "Even" : "Odd";\n}',
    python: 'def even_odd(n):\n    return "Even" if n % 2 == 0 else "Odd"',
    cpp: 'std::string evenOdd(int n) {\n  return (n % 2 == 0) ? "Even" : "Odd";\n}',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'A single modulo operation.',
  },
  {
    id: 'code-11', title: 'Leap Year', difficulty: 'easy',
    statement: 'Check if a year is a leap year.',
    approach: 'A leap year is divisible by 4, but not by 100 unless also by 400.',
    logic: 'year % 4 == 0 and (year % 100 != 0 or year % 400 == 0).',
    dryRun: '2000: divisible by 400 -> leap. 1900: divisible by 100 but not 400 -> not leap. 2024: divisible by 4, not 100 -> leap.',
    java: 'public static boolean isLeap(int year) {\n  return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);\n}',
    python: 'def is_leap(year):\n    return (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)',
    cpp: 'bool isLeap(int year) {\n  return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);\n}',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'A fixed set of modulo checks.',
  },
  {
    id: 'code-12', title: 'Multiplication Table', difficulty: 'easy',
    statement: 'Print the multiplication table of a number up to 10.',
    approach: 'Loop from 1 to 10 and multiply.',
    logic: 'for i in 1..10: print n * i.',
    dryRun: 'n = 5: 5x1=5, 5x2=10, ..., 5x10=50.',
    java: 'public static void table(int n) {\n  for (int i = 1; i <= 10; i++)\n    System.out.println(n + " x " + i + " = " + (n * i));\n}',
    python: 'def table(n):\n    for i in range(1, 11):\n        print(f"{n} x {i} = {n * i}")',
    cpp: 'void table(int n) {\n  for (int i = 1; i <= 10; i++) {\n    std::cout << n << " x " << i << " = " << (n * i) << std::endl;\n  }\n}',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'Fixed 10 iterations.',
  },
  {
    id: 'code-13', title: 'Swap Numbers', difficulty: 'easy',
    statement: 'Swap two numbers without using a third variable.',
    approach: 'Use addition/subtraction or XOR, or tuple unpacking in Python.',
    logic: 'a = a + b; b = a - b; a = a - b.',
    dryRun: 'a=3, b=5: a=8, b=3, a=5. Now a=5, b=3.',
    java: 'public static void swap(int a, int b) {\n  a = a + b;\n  b = a - b;\n  a = a - b;\n  System.out.println(a + " " + b);\n}',
    python: 'def swap(a, b):\n    a, b = b, a\n    return a, b',
    cpp: 'void swap(int a, int b) {\n  a = a + b;\n  b = a - b;\n  a = a - b;\n  std::cout << a << " " << b;\n}',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'A constant number of arithmetic operations.',
  },
  {
    id: 'code-14', title: 'Array Traversal', difficulty: 'easy',
    statement: 'Print all elements of an array.',
    approach: 'Loop through each index and print.',
    logic: 'for i in 0..n-1: print arr[i].',
    dryRun: 'arr = [10, 20, 30]: print 10, 20, 30.',
    java: 'public static void traverse(int[] arr) {\n  for (int x : arr) System.out.print(x + " ");\n}',
    python: 'def traverse(arr):\n    for x in arr:\n        print(x, end=" ")',
    cpp: 'void traverse(const std::vector<int>& arr) {\n  for (int x : arr) std::cout << x << " ";\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'We visit each element once.',
  },
  {
    id: 'code-15', title: 'Find Maximum', difficulty: 'easy',
    statement: 'Find the maximum element in an array.',
    approach: 'Track the largest seen so far while iterating.',
    logic: 'Initialize max to arr[0]; update if any element is larger.',
    dryRun: 'arr = [3, 7, 2, 9]: max=3 -> 7 -> 9.',
    java: 'public static int max(int[] arr) {\n  int max = arr[0];\n  for (int x : arr) if (x > max) max = x;\n  return max;\n}',
    python: 'def find_max(arr):\n    return max(arr)',
    cpp: 'int findMax(const std::vector<int>& arr) {\n  int max = arr[0];\n  for (int x : arr) {\n    if (x > max) max = x;\n  }\n  return max;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'One pass through the array.',
  },
  {
    id: 'code-16', title: 'Find Minimum', difficulty: 'easy',
    statement: 'Find the minimum element in an array.',
    approach: 'Track the smallest seen so far while iterating.',
    logic: 'Initialize min to arr[0]; update if any element is smaller.',
    dryRun: 'arr = [3, 7, 2, 9]: min=3 -> 2.',
    java: 'public static int min(int[] arr) {\n  int min = arr[0];\n  for (int x : arr) if (x < min) min = x;\n  return min;\n}',
    python: 'def find_min(arr):\n    return min(arr)',
    cpp: 'int findMin(const std::vector<int>& arr) {\n  int min = arr[0];\n  for (int x : arr) {\n    if (x < min) min = x;\n  }\n  return min;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'One pass through the array.',
  },
  {
    id: 'code-17', title: 'Sum of Array Elements', difficulty: 'easy',
    statement: 'Compute the sum of all elements in an array.',
    approach: 'Accumulate the sum while iterating.',
    logic: 'sum = 0; for each element, add to sum.',
    dryRun: 'arr = [1, 2, 3, 4]: sum = 10.',
    java: 'public static int sum(int[] arr) {\n  int sum = 0;\n  for (int x : arr) sum += x;\n  return sum;\n}',
    python: 'def sum_arr(arr):\n    return sum(arr)',
    cpp: 'int sum(const std::vector<int>& arr) {\n  int sum = 0;\n  for (int x : arr) sum += x;\n  return sum;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'One pass through the array.',
  },
  {
    id: 'code-18', title: 'String Reverse', difficulty: 'easy',
    statement: 'Reverse a given string.',
    approach: 'Swap characters from both ends or use built-in methods.',
    logic: 'Iterate from the end to the start, building a new string.',
    dryRun: 's = "hello": "o", "ol", "oll", "olle", "olleh".',
    java: 'public static String reverse(String s) {\n  return new StringBuilder(s).reverse().toString();\n}',
    python: 'def reverse(s):\n    return s[::-1]',
    cpp: 'std::string reverse(std::string s) {\n  std::reverse(s.begin(), s.end());\n  return s;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'Each character is visited once.',
  },
  {
    id: 'code-19', title: 'Linear Search', difficulty: 'easy',
    statement: 'Find the index of a target in an unsorted array.',
    approach: 'Check each element until the target is found.',
    logic: 'Loop and compare each element to the target.',
    dryRun: 'arr = [4, 2, 7, 1], target = 7: found at index 2.',
    java: 'public static int linearSearch(int[] arr, int target) {\n  for (int i = 0; i < arr.length; i++)\n    if (arr[i] == target) return i;\n  return -1;\n}',
    python: 'def linear_search(arr, target):\n    for i, x in enumerate(arr):\n        if x == target:\n            return i\n    return -1',
    cpp: 'int linearSearch(const std::vector<int>& arr, int target) {\n  for (int i = 0; i < arr.size(); i++) {\n    if (arr[i] == target) return i;\n  }\n  return -1;\n}',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'In the worst case we check every element.',
  },
  {
    id: 'code-20', title: 'Bubble Sort', difficulty: 'medium',
    statement: 'Sort an array in ascending order using bubble sort.',
    approach: 'Repeatedly swap adjacent out-of-order elements.',
    logic: 'Nested loops; each pass bubbles the largest unsorted element to the end.',
    dryRun: 'arr = [5, 2, 9, 1]: [2,5,1,9] -> [2,1,5,9] -> [1,2,5,9].',
    java: 'public static void bubbleSort(int[] arr) {\n  for (int i = 0; i < arr.length - 1; i++)\n    for (int j = 0; j < arr.length - 1 - i; j++)\n      if (arr[j] > arr[j + 1]) {\n        int t = arr[j]; arr[j] = arr[j + 1]; arr[j + 1] = t;\n      }\n}',
    python: 'def bubble_sort(arr):\n    n = len(arr)\n    for i in range(n - 1):\n        for j in range(n - 1 - i):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]',
    cpp: 'void bubbleSort(std::vector<int>& arr) {\n  int n = arr.size();\n  for (int i = 0; i < n - 1; i++) {\n    for (int j = 0; j < n - 1 - i; j++) {\n      if (arr[j] > arr[j + 1]) {\n        std::swap(arr[j], arr[j + 1]);\n      }\n    }\n  }\n}',
    timeComplexity: 'O(n^2)',
    bestCase: 'O(n)',
    avgCase: 'O(n^2)',
    worstCase: 'O(n^2)',
    spaceComplexity: 'O(1)',
    explanation: 'Two nested loops over the array.',
  },
];
