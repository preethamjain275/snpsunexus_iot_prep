import type { CodingQuestion } from '@/types';

export const codingQuestions2: CodingQuestion[] = [
  {
    id: 'code-21', title: 'Selection Sort', difficulty: 'medium',
    statement: 'Sort an array in ascending order using selection sort by repeatedly selecting the minimum.',
    approach: 'Find the smallest element in the unsorted part and swap it to the front.',
    logic: 'For each position i, scan the rest for the minimum and swap it into i.',
    dryRun: 'arr = [5,2,9,1]: i=0 min=1 -> [1,2,9,5]; i=1 min=2 -> [1,2,9,5]; i=2 min=5 -> [1,2,5,9].',
    java: 'public static void selectionSort(int[] a) {\n  for (int i = 0; i < a.length - 1; i++) {\n    int min = i;\n    for (int j = i + 1; j < a.length; j++) if (a[j] < a[min]) min = j;\n    int t = a[i]; a[i] = a[min]; a[min] = t;\n  }\n}',
    python: 'def selection_sort(a):\n    for i in range(len(a) - 1):\n        m = i\n        for j in range(i + 1, len(a)):\n            if a[j] < a[m]: m = j\n        a[i], a[m] = a[m], a[i]',
    timeComplexity: 'O(n^2)',
    bestCase: 'O(n^2)',
    avgCase: 'O(n^2)',
    worstCase: 'O(n^2)',
    spaceComplexity: 'O(1)',
    explanation: 'Always n(n-1)/2 comparisons regardless of input order.',
  },
  {
    id: 'code-22', title: 'Insertion Sort', difficulty: 'medium',
    statement: 'Sort an array by inserting each element into its correct position in the sorted part.',
    approach: 'Build the sorted array one item at a time by inserting into place.',
    logic: 'For each element, shift larger sorted elements right and insert it.',
    dryRun: 'arr = [5,2,9,1]: 2<5 -> [2,5,9,1]; 9 ok; 1 shifts -> [1,2,5,9].',
    java: 'public static void insertionSort(int[] a) {\n  for (int i = 1; i < a.length; i++) {\n    int key = a[i], j = i - 1;\n    while (j >= 0 && a[j] > key) { a[j+1] = a[j]; j--; }\n    a[j+1] = key;\n  }\n}',
    python: 'def insertion_sort(a):\n    for i in range(1, len(a)):\n        key = a[i]; j = i - 1\n        while j >= 0 and a[j] > key:\n            a[j+1] = a[j]; j -= 1\n        a[j+1] = key',
    timeComplexity: 'O(n^2), O(n) best',
    bestCase: 'O(n)',
    avgCase: 'O(n^2)',
    worstCase: 'O(n^2)',
    spaceComplexity: 'O(1)',
    explanation: 'Efficient for nearly sorted data; best case is O(n).',
  },
  {
    id: 'code-23', title: 'Binary Search', difficulty: 'medium',
    statement: 'Find the index of a target in a sorted array using binary search.',
    approach: 'Repeatedly halve the search range by comparing to the middle.',
    logic: 'low=0, high=n-1; while low<=high, mid=(low+high)/2; adjust range.',
    dryRun: 'arr=[1,3,5,7,9], target=7: mid=2(5)<7 -> low=3; mid=3(7)==7 -> found.',
    java: 'public static int binarySearch(int[] a, int t) {\n  int lo = 0, hi = a.length - 1;\n  while (lo <= hi) {\n    int mid = (lo + hi) / 2;\n    if (a[mid] == t) return mid;\n    if (a[mid] < t) lo = mid + 1; else hi = mid - 1;\n  }\n  return -1;\n}',
    python: 'def binary_search(a, t):\n    lo, hi = 0, len(a) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if a[mid] == t: return mid\n        if a[mid] < t: lo = mid + 1\n        else: hi = mid - 1\n    return -1',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Halves the range each step; requires a sorted array.',
  },
  {
    id: 'code-24', title: 'GCD of Two Numbers', difficulty: 'medium',
    statement: 'Find the greatest common divisor of two numbers using Euclid\'s algorithm.',
    approach: 'Repeatedly replace (a,b) with (b, a%b) until b is 0.',
    logic: 'gcd(a,b) = gcd(b, a%b); base case gcd(a,0) = a.',
    dryRun: 'a=12, b=8: gcd(12,8)->gcd(8,4)->gcd(4,0)=4.',
    java: 'public static int gcd(int a, int b) {\n  while (b != 0) { int t = b; b = a % b; a = t; }\n  return a;\n}',
    python: 'def gcd(a, b):\n    while b:\n        a, b = b, a % b\n    return a',
    timeComplexity: 'O(log min(a,b))',
    bestCase: 'O(log min(a,b))',
    avgCase: 'O(log min(a,b))',
    worstCase: 'O(log min(a,b))',
    spaceComplexity: 'O(1)',
    explanation: 'Euclid\'s algorithm reduces the problem size each step.',
  },
  {
    id: 'code-25', title: 'LCM of Two Numbers', difficulty: 'medium',
    statement: 'Find the least common multiple of two numbers.',
    approach: 'Use the relation LCM(a,b) = (a*b) / GCD(a,b).',
    logic: 'Compute GCD first, then divide the product by it.',
    dryRun: 'a=4, b=6: gcd=2, lcm=24/2=12.',
    java: 'public static int lcm(int a, int b) {\n  return (a * b) / gcd(a, b);\n}',
    python: 'def lcm(a, b):\n    return (a * b) // gcd(a, b)',
    timeComplexity: 'O(log min(a,b))',
    bestCase: 'O(log min(a,b))',
    avgCase: 'O(log min(a,b))',
    worstCase: 'O(log min(a,b))',
    spaceComplexity: 'O(1)',
    explanation: 'Reduces to GCD computation via the product/gcd relation.',
  },
  {
    id: 'code-26', title: 'Power of a Number', difficulty: 'medium',
    statement: 'Compute x raised to the power n efficiently.',
    approach: 'Use binary exponentiation (exponentiation by squaring).',
    logic: 'If n is even, x^n = (x^(n/2))^2; if odd, multiply by x once.',
    dryRun: 'x=2, n=10: 2^10 = (2^5)^2 = 32^2 = 1024.',
    java: 'public static double power(double x, int n) {\n  if (n == 0) return 1;\n  double half = power(x * x, n / 2);\n  return (n % 2 == 0) ? half : x * half;\n}',
    python: 'def power(x, n):\n    if n == 0: return 1\n    half = power(x * x, n // 2)\n    return half if n % 2 == 0 else x * half',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Halves the exponent each step instead of n multiplications.',
  },
  {
    id: 'code-27', title: 'Check Anagram', difficulty: 'medium',
    statement: 'Check if two strings are anagrams (same characters, same counts).',
    approach: 'Count character frequencies and compare.',
    logic: 'Sort both strings and compare, or use frequency arrays.',
    dryRun: '"listen" and "silent" -> sorted both = "eilnst" -> anagrams.',
    java: 'public static boolean isAnagram(String a, String b) {\n  char[] x = a.toCharArray(), y = b.toCharArray();\n  Arrays.sort(x); Arrays.sort(y);\n  return Arrays.equals(x, y);\n}',
    python: 'def is_anagram(a, b):\n    return sorted(a) == sorted(b)',
    timeComplexity: 'O(n log n)',
    bestCase: 'O(n log n)',
    avgCase: 'O(n log n)',
    worstCase: 'O(n log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Sorting both strings and comparing is simple; counting is O(n).',
  },
  {
    id: 'code-28', title: 'Count Vowels and Consonants', difficulty: 'easy',
    statement: 'Count the number of vowels and consonants in a string.',
    approach: 'Iterate and check each character against the vowel set.',
    logic: 'For each char, if in "aeiou" increment vowels, else consonants (ignore non-letters).',
    dryRun: '"hello": vowels e,o=2; consonants h,l,l=3.',
    java: 'public static int[] countVC(String s) {\n  int v = 0, c = 0;\n  s = s.toLowerCase();\n  for (char ch : s.toCharArray()) {\n    if ("aeiou".indexOf(ch) >= 0) v++;\n    else if (Character.isLetter(ch)) c++;\n  }\n  return new int[]{v, c};\n}',
    python: 'def count_vc(s):\n    s = s.lower(); v = set("aeiou")\n    vowels = sum(1 for ch in s if ch in v)\n    cons = sum(1 for ch in s if ch.isalpha() and ch not in v)\n    return vowels, cons',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'One pass through the string with constant-time membership checks.',
  },
  {
    id: 'code-29', title: 'Reverse an Array', difficulty: 'easy',
    statement: 'Reverse the elements of an array in place.',
    approach: 'Swap elements from both ends moving toward the center.',
    logic: 'Two pointers: swap arr[i] and arr[n-1-i] for i < n/2.',
    dryRun: '[1,2,3,4] -> swap 1,4 -> [4,2,3,1] -> swap 2,3 -> [4,3,2,1].',
    java: 'public static void reverse(int[] a) {\n  for (int i = 0; i < a.length / 2; i++) {\n    int t = a[i]; a[i] = a[a.length - 1 - i]; a[a.length - 1 - i] = t;\n  }\n}',
    python: 'def reverse(a):\n    for i in range(len(a) // 2):\n        a[i], a[-1-i] = a[-1-i], a[i]',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'Each swap is O(1); n/2 swaps total.',
  },
  {
    id: 'code-30', title: 'Find Second Largest', difficulty: 'medium',
    statement: 'Find the second largest element in an array in one pass.',
    approach: 'Track the largest and second largest while iterating.',
    logic: 'If current > largest, update second = largest, largest = current; else if > second, update second.',
    dryRun: '[3,7,2,9]: largest=3->7->9; second=-inf->3->7. Second=7.',
    java: 'public static int secondLargest(int[] a) {\n  int first = Integer.MIN_VALUE, second = Integer.MIN_VALUE;\n  for (int x : a) {\n    if (x > first) { second = first; first = x; }\n    else if (x > second && x != first) second = x;\n  }\n  return second;\n}',
    python: 'def second_largest(a):\n    first = second = float("-inf")\n    for x in a:\n        if x > first:\n            second = first; first = x\n        elif x > second and x != first:\n            second = x\n    return second',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'One pass with two trackers; handles duplicates by skipping equals.',
  },
  {
    id: 'code-31', title: 'Check String Palindrome', difficulty: 'easy',
    statement: 'Check if a string reads the same forwards and backwards.',
    approach: 'Compare characters from both ends moving inward.',
    logic: 'Two pointers; if any pair differs, not a palindrome.',
    dryRun: '"madam": m==m, a==a, d middle -> palindrome.',
    java: 'public static boolean isPalindrome(String s) {\n  int i = 0, j = s.length() - 1;\n  while (i < j) if (s.charAt(i++) != s.charAt(j--)) return false;\n  return true;\n}',
    python: 'def is_palindrome(s):\n    return s == s[::-1]',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(1)',
    explanation: 'Compare up to n/2 character pairs.',
  },
  {
    id: 'code-32', title: 'Fibonacci (Recursive)', difficulty: 'medium',
    statement: 'Compute the nth Fibonacci number recursively.',
    approach: 'Use the recurrence fib(n) = fib(n-1) + fib(n-2) with base cases.',
    logic: 'Base: fib(0)=0, fib(1)=1; else recurse.',
    dryRun: 'fib(5) = fib(4)+fib(3) = 3+2 = 5.',
    java: 'public static int fib(int n) {\n  if (n <= 1) return n;\n  return fib(n - 1) + fib(n - 2);\n}',
    python: 'def fib(n):\n    if n <= 1: return n\n    return fib(n - 1) + fib(n - 2)',
    timeComplexity: 'O(2^n)',
    bestCase: 'O(2^n)',
    avgCase: 'O(2^n)',
    worstCase: 'O(2^n)',
    spaceComplexity: 'O(n)',
    explanation: 'Naive recursion recomputes subproblems; use memoization for O(n).',
  },
  {
    id: 'code-33', title: 'Sum of Natural Numbers', difficulty: 'easy',
    statement: 'Compute the sum of the first n natural numbers.',
    approach: 'Use the formula n*(n+1)/2 or a loop.',
    logic: 'Formula avoids a loop and is O(1).',
    dryRun: 'n=5: 5*6/2 = 15.',
    java: 'public static int sumN(int n) {\n  return n * (n + 1) / 2;\n}',
    python: 'def sum_n(n):\n    return n * (n + 1) // 2',
    timeComplexity: 'O(1)',
    bestCase: 'O(1)',
    avgCase: 'O(1)',
    worstCase: 'O(1)',
    spaceComplexity: 'O(1)',
    explanation: 'The arithmetic formula avoids iteration entirely.',
  },
  {
    id: 'code-34', title: 'Factorial (Recursive)', difficulty: 'medium',
    statement: 'Compute n! recursively.',
    approach: 'Use n! = n * (n-1)! with 0! = 1.',
    logic: 'Base case 0! = 1; recurse otherwise.',
    dryRun: 'fact(5) = 5*4*3*2*1 = 120.',
    java: 'public static long fact(int n) {\n  if (n <= 1) return 1;\n  return n * fact(n - 1);\n}',
    python: 'def fact(n):\n    if n <= 1: return 1\n    return n * fact(n - 1)',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(n)',
    explanation: 'n recursive calls; watch for stack overflow on large n.',
  },
  {
    id: 'code-35', title: 'Remove Duplicates from Array', difficulty: 'medium',
    statement: 'Remove duplicate elements from an array, preserving order.',
    approach: 'Use a set to track seen elements.',
    logic: 'Add to result only if not already seen.',
    dryRun: '[1,2,2,3,1] -> [1,2,3].',
    java: 'public static int[] removeDup(int[] a) {\n  java.util.LinkedHashSet<Integer> s = new java.util.LinkedHashSet<>();\n  for (int x : a) s.add(x);\n  return s.stream().mapToInt(Integer::intValue).toArray();\n}',
    python: 'def remove_dup(a):\n    seen = set(); out = []\n    for x in a:\n        if x not in seen:\n            seen.add(x); out.append(x)\n    return out',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(n)',
    explanation: 'Set lookups are O(1) on average; preserves insertion order.',
  },
  {
    id: 'code-36', title: 'Check Armstrong (n digits)', difficulty: 'medium',
    statement: 'Check if an n-digit number equals the sum of its digits each raised to n.',
    approach: 'Count digits, sum powered digits, compare.',
    logic: 'Same as the 3-digit version but using the digit count as the power.',
    dryRun: '9474 (4 digits): 9^4+4^4+7^4+4^4 = 6561+256+2401+256 = 9474 -> Armstrong.',
    java: 'public static boolean isArmstrong(int n) {\n  int d = String.valueOf(n).length(), sum = 0, t = n;\n  while (t > 0) { sum += Math.pow(t % 10, d); t /= 10; }\n  return sum == n;\n}',
    python: 'def is_armstrong(n):\n    d = len(str(n))\n    return n == sum(int(x) ** d for x in str(n))',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Processes each digit once; the count of digits is log10(n).',
  },
  {
    id: 'code-37', title: 'Perfect Number', difficulty: 'medium',
    statement: 'Check if a number equals the sum of its proper divisors (e.g., 6 = 1+2+3).',
    approach: 'Sum divisors from 1 to sqrt(n) and add pairs.',
    logic: 'For each i dividing n, add i and n/i (avoid double-counting sqrt).',
    dryRun: 'n=28: 1+2+4+7+14 = 28 -> perfect.',
    java: 'public static boolean isPerfect(int n) {\n  int sum = 1;\n  for (int i = 2; i * i <= n; i++) if (n % i == 0) { sum += i; if (i != n/i) sum += n/i; }\n  return sum == n && n != 1;\n}',
    python: 'def is_perfect(n):\n    if n < 2: return False\n    s = 1\n    for i in range(2, int(n**0.5) + 1):\n        if n % i == 0:\n            s += i\n            if i != n // i: s += n // i\n    return s == n',
    timeComplexity: 'O(sqrt(n))',
    bestCase: 'O(sqrt(n))',
    avgCase: 'O(sqrt(n))',
    worstCase: 'O(sqrt(n))',
    spaceComplexity: 'O(1)',
    explanation: 'Check divisors up to sqrt(n) and add both members of each pair.',
  },
  {
    id: 'code-38', title: 'Prime Numbers in a Range', difficulty: 'medium',
    statement: 'Print all prime numbers between two given numbers.',
    approach: 'For each number in the range, test primality up to its square root.',
    logic: 'Reuse the isPrime check for each candidate.',
    dryRun: 'Range 10-20: 11, 13, 17, 19.',
    java: 'public static void primes(int lo, int hi) {\n  for (int n = lo; n <= hi; n++) if (isPrime(n)) System.out.print(n + " ");\n}',
    python: 'def primes(lo, hi):\n    for n in range(lo, hi + 1):\n        if is_prime(n): print(n, end=" ")',
    timeComplexity: 'O((hi-lo) * sqrt(n))',
    bestCase: 'O((hi-lo) * sqrt(n))',
    avgCase: 'O((hi-lo) * sqrt(n))',
    worstCase: 'O((hi-lo) * sqrt(n))',
    spaceComplexity: 'O(1)',
    explanation: 'Each candidate is tested for primality in O(sqrt(n)).',
  },
  {
    id: 'code-39', title: 'Decimal to Binary', difficulty: 'medium',
    statement: 'Convert a decimal number to its binary representation.',
    approach: 'Repeatedly divide by 2 and collect remainders.',
    logic: 'Build the binary string from remainders in reverse.',
    dryRun: 'n=13: 13%2=1, 6%2=0, 3%2=1, 1%2=1 -> "1101".',
    java: 'public static String toBinary(int n) {\n  if (n == 0) return "0";\n  StringBuilder b = new StringBuilder();\n  while (n > 0) { b.insert(0, n % 2); n /= 2; }\n  return b.toString();\n}',
    python: 'def to_binary(n):\n    return bin(n)[2:]',
    timeComplexity: 'O(log n)',
    bestCase: 'O(log n)',
    avgCase: 'O(log n)',
    worstCase: 'O(log n)',
    spaceComplexity: 'O(1)',
    explanation: 'Number of bits is log2(n).',
  },
  {
    id: 'code-40', title: 'Find Duplicates in Array', difficulty: 'medium',
    statement: 'Find all duplicate elements in an array.',
    approach: 'Use a frequency map to find elements appearing more than once.',
    logic: 'Count occurrences; collect keys with count > 1.',
    dryRun: '[1,2,3,2,1,4] -> duplicates [2,1].',
    java: 'public static java.util.List<Integer> findDup(int[] a) {\n  java.util.Map<Integer,Integer> m = new java.util.HashMap<>();\n  for (int x : a) m.merge(x, 1, Integer::sum);\n  java.util.List<Integer> d = new java.util.ArrayList<>();\n  for (var e : m.entrySet()) if (e.getValue() > 1) d.add(e.getKey());\n  return d;\n}',
    python: 'from collections import Counter\ndef find_dup(a):\n    c = Counter(a)\n    return [k for k, v in c.items() if v > 1]',
    timeComplexity: 'O(n)',
    bestCase: 'O(n)',
    avgCase: 'O(n)',
    worstCase: 'O(n)',
    spaceComplexity: 'O(n)',
    explanation: 'One pass to count, one pass to collect duplicates.',
  },
];
